services:
  - type: web
    name: dash-mpd-server
    env: node
    plan: free
    buildCommand: |
      apt-get update && apt-get install -y ffmpeg
      npm install
    startCommand: node server.js
